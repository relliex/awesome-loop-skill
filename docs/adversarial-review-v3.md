# v3 升级 · 对抗验证报告（AC9 交付物）

- 日期：2026-09-17
- 判据：`docs/upgrade-to-v3-requirements.md` 的 R1–R10 与 AC1–AC8
- 验证方式：独立子代理实例（未参与任何交付物撰写，看不到编排方对话），
  任务为"证明本次交付**不满足**判据"；两轮：第一轮找问题，第二轮仅复核
  修复（复核阶段全程只读）
- 结论：机制层与教学层经得起机械核查；两轮共发现 24 项问题（F1–F13、
  N1–N11），**全部处置完毕**（23 项修复 + 1 项记录为受控设计）。最终
  SKILL.md 406 行（AC6 上限 500）；修复后复核结论："可以提交推送"。

## 交付物与判据对照（最终状态）

| 条目 | 判定 | 关键证据 |
|------|------|----------|
| R1 域无关 | 满足 | SKILL.md 主干为三种原型表 + 门表；v2 岗位缩写全量 grep 仅 1 命中（YAML 注释示例） |
| R2 岗位生成教学 | 满足 | §2 生成八步 + 自检清单；role-design.md 8 领域案例，每例含角色清单/对抗配对/完整提示词 |
| R3 需求完整性分析 | 满足 | §5.1 八维度清单 + B1 四类缺口（抽象/缺失/矛盾/不可判定） |
| R4 多轮选择器澄清 | 满足 | §3 协议 + 问句模板 + 能力门含选择器检查 |
| R5 方案确认门 | 满足 | G2 行（时机/必含信息/失败路径）+ "是否接受当前方案？" |
| R6 团队编排门 | 满足 | C1–C3 + team.md 字段（含预算分摊、派发顺序）+ "是否同意？" |
| R7 满意判定与迭代 | 满足 | "含糊答复不算通过"双向定义 |
| R8 循环运行 | 满足 | D 段 + 并行派发示例 |
| R9 不回退 | 满足 | 与 v2 逐机制对照无删减；v3 新增 team.md 入锚定重读序 |
| R10 仓库同步 | 满足 | 本次提交推送；双语 README、Description、报告同步 |
| AC1 无固定岗位主干 | 满足 | 表格扫描仅 3 处，无岗位名单表 |
| AC2 ≥6 案例三要素 | 满足 | 8 案例逐块核对五要素，无空壳 |
| AC3 三门定义齐备 | 满足 | 三门 × 时机/必含信息/失败路径 |
| AC4 team.md 字段定义 | 满足 | 示例 + 派发顺序 + 门记录字段（键名已统一为 `gates`） |
| AC5 ≥6 维度可勾选 | 满足 | 8 个 `- [ ]` 维度 |
| AC6 ≤500 行 + frontmatter | 满足 | 修复后 406 行；name 合规；description 覆盖四阶段 |
| AC7 三机制可检索未削弱 | 满足 | anchors 逐字摘录、连续 CLEAN 计数（词表已定义）、预算不重置 |
| AC8 双语 README + Description | 满足 | 门编号一致、Description 同步、仓库结构含 docs |
| AC9 对抗自检报告 | 满足 | 本文件（含两轮发现与逐项处置） |

## 机械验证方法（验证者实际执行的命令）

```bash
wc -l SKILL.md                                   # 386 → 修复后 406（AC6）
head -12 SKILL.md                                # frontmatter 合规性
grep -nE 'PM|ARCH|DEV|REV|QA|FIX|AUD|DOC' SKILL.md   # R1：仅 YAML 注释 1 命中
grep -c '^```text' references/role-design.md     # AC2：8 个提示词块
grep -n '^## ' references/role-design.md         # AC2：8 个领域案例
awk 逐案例切块核对五要素（交付物/判据来源/角色清单/对抗配对/提示词）
git show HEAD:SKILL.md > /tmp/v2-skill.md        # R9：与 v2 逐机制对照
grep -rn '验收计划\|design.md' SKILL.md references/   # F6 术语与断链
git status --short ; git log --oneline           # R10 同步状态
WebFetch github.com/relliex/awesome-loop-skill   # R10 远端 Description
find . -iname '*report*' -o -iname '*自检*'       # AC9 报告产物
```

## 第一轮发现与处置（F1–F13）

| ID | 严重度 | 问题 | 处置 |
|----|--------|------|------|
| F1 | 高 | 双语 README 流程块门编号错乱（团队标 C、运行标 G3），与同文件后文矛盾 | 已修复：改为 G1/G2/G3 + RUN |
| F2 | 高 | v3 未提交推送，远端仍是 v2 | 已闭环：本次提交推送 |
| F3 | 中 | AC9 报告无产物 | 已修复：本文件 |
| F4 | 中 | R3 要求的"抽象"缺口类型未实现 | 已修复：B1 四类缺口 |
| F5 | 中 | G1 轮数上限后无出路（流程死路） | 已修复：届满挂起，不得带病冻结 |
| F6 | 中 | 引用 v3 已不存在的 `design.md`；"验收计划"与 `acceptance.md` 术语冲突 | 已修复：增可选 design.md 并接入治理链；术语统一 |
| F7 | 中 | role-catalog 把准出标准撰写给"主模型 + PM"，与"主模型不产出交付物"冲突 | 已修复：改由方案/判据角色撰写，主模型仅登记 |
| F8 | 低 | README 自称"不带岗位库"却随仓发布岗位库 | 已修复：改为"不以岗位库为主干 + 永非必读附录" |
| F9 | 低 | G1 门记录缺 `user_words`，与门规则口径不一 | 已修复：字段补齐、键名统一为 `gates` |
| F10 | 低 | `CLEAN` 令牌消失、`FOUND` 无对立值定义 | 已修复：两文件显式定义判定词表 |
| F11 | 低 | 需求点名的问句未逐字出现，问法可能漂移 | 已修复：增问句模板，采用用户原话问法 |
| F12 | 低 | 高风险默认清单被移除 | 已修复：恢复域无关高风险默认清单 |
| F13 | 低 | 降级模式重新引入自评路径 | 记录为受控设计并加固：需用户明确同意 + 双重标注 + 人工可复核步骤 + 高风险禁用 + 显式豁免声明 |

## 第二轮复核发现与处置（N1–N11）

复核对象为第一轮修复是否到位、是否引入新矛盾；结论：F1/F4/F5/F7/F8/F11/
F12 已解决，F3/F6/F9/F10/F13 部分解决（均由下列 N 项承接），F2 待提交。

| ID | 严重度 | 问题 | 处置 |
|----|--------|------|------|
| N1 | 中 | 门记录键名 `approvals` 与 `gates` 不一致，校验/恢复读不到门记录 | 已修复：统一为 `gates` |
| N2 | 中 | `design.md` 进了目录树但未接入治理链（冻结/锚点/版本/恢复均无它） | 已修复：四处全部接入（标注"若启用"） |
| N3 | 中 | 报告含指向不存在章节的空占位符 | 已修复：本节即为复验结论 |
| N4 | 低 | 报告摘要计数与自身表格不一致 | 已修复：本版按实际逐项重计 |
| N5 | 低 | 判定词表未全域统一；`status` 与结论词表关系未定义 | 已修复：补两词表关系说明；role-design.md 增统一声明 |
| N6 | 低 | README 未反映降级模式受控例外 | 已修复：双语 README 补充说明 |
| N7 | 低 | 降级模式与"反自导自演"冲突无交叉引用；"高风险项目"前向悬空 | 已修复：显式豁免声明 + 指向 §7 抄高风险清单 |
| N8 | 低 | README 仓库结构未列本次新增的 docs 报告 | 已修复：双语 README 补齐两条 docs |
| N9 | 低 | 报告行数证据停留在修复前（386） | 已修复：本版记录修复后 406 行 |
| N10 | 低 | "每问必有推荐项"与二值批准问句的表述张力 | 已修复：限定为"缺口澄清题"，G2/G3 单独说明 |
| N11 | 低 | AC9 独立性无过程证据 | 已修复：见下方派发记录 |

## 验证者重点攻击但未能推翻的项

1. **AC1**：v2 岗位缩写全量 grep + 表格扫描，未发现成建制岗位表；
2. **AC6**：行数（修复后 406，留 19% 余量）；
3. **AC2**：逐案例切块核对五要素，无空壳案例；
4. **AC7**：与 v2 逐机制对照，未见删减或语义削弱；
5. **门禁绕过**：构造跳过 G1/G2/G3 的路径，被"禁止代答跳过 + 未记录即
   pending + 恢复检查单"三处成链挡住；
6. **自评黑洞**：team.md 每个 producer 行都有对抗配对，未能构造仅生产者
   可验证的角色。

## 附：派发记录（N11 的过程证据）

- 实例：`general-purpose` 子代理，两轮（第一轮全面对抗验证；第二轮只读
  复核）。它没有也不曾接触编排方的对话上下文，输入仅为本需求文档与
  仓库内交付物文件——这正是本 skill 主张的信息隔离在本次升级自身上的
  一次实际应用。
- 第一轮输入：判据全文 + 6 个交付物文件；要求逐条判定、机械验证、
  报告可复现证据，禁止修改文件。
- 第二轮输入：13 项处置清单 + 同样的只读约束；要求给出"已解决/未解决/
  部分解决"并只报告残留与新引入问题。
- 两轮返回：逐条判定表 + 问题清单（含严重度、位置、证据、期望 vs 实际）
  + "攻击但未能推翻"清单 + 提交建议。
- 编排方处置原则：所有阻断与中severity 发现一律修复后才提交；低
  severity 全部修复；唯一未按"修复"处理的是 F13（保留为受控逃生门，
  加固并记录理由）。
