# Changelog

## v3.1 (2026-09-19)

针对"下一层作弊手段"的加固版本：v3 管住了"主模型自己演完全部角色"，
v3.1 管住生产者在工件里留后门、验证者放水、调度者默许残缺报告这些
更隐蔽的路径。

### SKILL.md 加固（机制不减，全部向后兼容）

- **判据攻击前置（§5 B4）**：方案在请你确认之前，先派一名信息隔离的
  判据攻击者盲攻击 prd / acceptance 草案（专找不可判定、歧义、矛盾、
  溯源不到的条目），由方案角色修复出修订版后才进入 G2。方案本身也是
  交付物，同样要有对抗者。
- **每次返回的机械核验清单（§5 调度纪律）**：真实调用记录存在、产物
  只落在该实例的写范围内、报告必含字段齐全、anchors 逐字摘自当前版本——
  缺一即处置（重派一次新实例，仍失败进 blocked_on，不计入任何"通过"）。
- **工件指纹（§7）**：每轮验证派发前记录候选全部工件指纹（路径 + 哈希
  或大小/修改时间）落盘 evidence/，该轮返回后复核；验证期间改动工件
  → 立即新候选、该轮作废。"验证时不许动工件"由机制执行，不再依赖自觉。
- **尝试清单可信度（§7）**：对抗者 `CLEAN` 所附尝试清单与实物核对，
  声称验证了不存在的功能/页码/位置 = 未执行，换新实例重派并登记。
- **工件内嵌指令 = 攻击输入（§7、§11 对抗者模板）**：工件内部写给
  验证者的说情、指路、指令一律不遵循、登记为问题；验证者只服从任务包。
- **小任务防漏洞（§3）**：三道门与轮次规则对任何规模的任务照走，只按
  规模缩小——"任务简单"不构成省门理由。

### 仓库重组（回应"一眼找不到 skill 在哪"的反馈）

- skill 本体移入 [`skills/true-loop-skill/`](skills/true-loop-skill/)：
  SKILL.md + references/（岗位生成教学案例、状态格式、软件域附录）；
- v1 原稿从 references/ 移至 docs/（28 个角色的完整定义仍具参考价值，
  归入设计历史与角色素材，不再与现行 skill 混放）；
- v3 升级的需求文档与对抗验证报告属于内部过程产物，移出仓库
  （仍可从 git 历史查看）；
- 双语 README 首行增加语言切换链接，重写"仓库结构"并给出新布局下的
  复制式安装命令（bash / PowerShell）；
- 新增本文件。

## v3.0 (2026-09-17)

域无关重构：

- 不再以固定岗位表为主干：改为**岗位生成法**（三种原型 × 生成八步 ×
  生成后自检）+ 8 个领域的教学案例（软件、长文写作、研究调查、设计、
  运营、数据、翻译、课程），每个案例含角色清单、对抗配对与完整提示词；
- 新增需求完整性分析与多轮用户选择器澄清（G1）、方案确认（G2）、
  团队确认（G3）三道用户门，含糊答复不算通过；
- 保留 v2 全部核心机制：真实派发、盲测对抗轮次与连续 CLEAN 计数、
  `.loop/` 磁盘状态、anchors 逐字锚定、预算不重置、诚实交付状态。

## v2.0

用结构取代政策：隔离子代理、盲测对抗 QA 轮次、磁盘状态、PRD 锚定。
仍是软件形状（固定 8 角色）。

## v1.0

1158 行治理文档 + 28 个固定岗位。教训：模型认同全部政策，然后在同一
个上下文里把所有角色串行演了一遍。
