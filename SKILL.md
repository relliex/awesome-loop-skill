---
name: true-loop-skill
description: >
  对抗式子代理工程闭环。主模型只做编排与机械核验，PRD、设计、
  开发、审阅、破坏性测试、审计全部交给相互隔离的真实子代理
  并行执行；用连续盲测轮次与证据门禁对抗模型自导自演和
  纸面达标；以用户访谈沉淀的 PRD 为每轮锚点，对抗上下文
  压缩导致的需求稀释。当用户要求工程闭环、真循环、多角色
  协作开发、独立测试验收、"让子代理互相对抗"、"别让模型
  自己写自己测"，或启动中大型软件项目的自动开发循环时使用。
---

# True Loop：对抗式子代理工程闭环

## 0 设计原理

为什么必须用隔离的子代理，而不是主模型深度自循环：

1. **隔离产生诚实。** 子代理看不到主对话，它的全部世界就是任务包。
   开发实例的唯一目标是把分到的实现做出来；测试实例的唯一目标是
   用实际运行证明项目不达标。两者天然对抗，开发会想方设法完成，
   测试会想方设法找茬——这比任何"请严格检查"的恳求都有效。
2. **主模型亲自执行必然污染验证。** 同一个上下文里既有开发的辛苦
   又有验收标准，"完成欲"会驱使模型降低标准、美化结果、用自评
   替代实测——这正是纸面数据漂亮、实际项目不可用的根源。所以
   主模型不写产品代码、不写测试、不写 PRD，只做编排、核验证据、
   路由缺陷。
3. **只认产物与证据，不认转述。** 任何"通过"必须有落盘的命令输出、
   复现步骤或独立报告支撑；没有真实子代理调用记录的阶段产物无效。

本 skill 的每条规则都必须落到一次真实子代理调用或一个磁盘产物上——
结构先于口号。

## 1 硬性门禁与自主边界（P0 第一件事）

启动前逐项检查，全部满足才继续：

1. 宿主提供真实子代理工具（Agent / Task 等），支持在一条消息里
   并行发起多个调用，并返回真实结果；
2. 文件读写与命令执行可用；
3. 能持久化 `.loop/` 状态目录。

任一不满足 → 触发 `BLOCKED_CAPABILITY`：先把门禁结果、证据与待决策
项写入 state.json 的 `gate_p0` 与 `blocked_on`（挂起语义，保证可恢复），
再向用户报告并停止。禁止用角色扮演模拟子代理；禁止把"回复已结束"
描述成"仍在后台运行"。用户明确接受时可降级为串行自检模式，但一切
产出必须标注"未经独立子代理验证"。

**探针派发**：门禁不能只靠静态判断——工具存在不等于链路可用。P0
必须包含一次最小真实派发（例如让一个只读子代理回报目标目录内容），
验证派发与返回真实可用；需要并行时同一条消息派两个探针一并验证。
探针计入预算。

**执行者约束**：本 skill 只由主模型加载执行。子代理只接收任务包，
不读本 skill；宿主通常不允许子代理嵌套派发，所以任务包绝不要求
子代理"再派其他角色"——任务包之外的问题一律以 unresolved_questions
带回，由主模型路由。

**反自导自演**：每个阶段的产物必须伴随真实子代理调用记录；没有调用
记录的产物无效，必须重派。主模型一旦发现自己在写产品代码、测试或
PRD：立即停止、作废该工作、改派子代理。

**自主边界**：在批准的范围、权限与预算内自动推进，不要每一步都问
"继续吗"。仅以下情况必须等待用户：阻断性业务决策、验收标准或关键
契约的变更、新增权限或预算、生产部署与破坏性操作授权。

## 2 角色体系

核心 8 类角色。每类是一个"实例工厂"：按任务包重新派出，可多实例并行。

| 角色 | 目标函数（实例心中只有这个） | 任务包信息边界 |
|------|------------------------------|----------------|
| PM   | 把用户目标变成可判定的验收标准 | 用户目标、现状调查、用户决策记录（P1c 定稿时） |
| ARCH | 把验收标准变成可实现可测试的设计 | PRD、代码库调查 |
| DEV  | 在写权限内实现，让构建与自测真实通过 | PRD、设计、写范围 |
| REV  | 找出实现相对设计的偏差与风险 | PRD、设计、完整 diff；无开发辩解 |
| QA   | 用实际运行证明项目不达标 | 验收标准、运行方法；绝不给开发报告与既往轮次 |
| FIX  | 消除指定缺陷且不引入新问题 | 缺陷包（含复现）、相关代码 |
| AUD  | 证明交付追踪链完整，或指出断裂 | 全部产物索引与证据 |
| DOC  | 让交接材料与真实行为一致 | 最终实现、运行方式 |

专项变体按需以 DEV/QA/REV/ARCH 变体派出：安全、性能、数据与迁移、
运维、发布、UX/UI。原 28 岗位与专项的完整映射见
[references/role-catalog.md](references/role-catalog.md)，仅大型项目需要读。

隔离规则：

- 作者与审阅者、修复者与复验者不得是同一实例；
- 每轮测试使用全新实例（重新派任务包，不是继续旧对话）；
- **QA 盲测**：QA 任务包不得包含开发自评、既往测试报告、已知缺陷
  清单——防止锚定，让它从零开始找茬。

## 3 磁盘状态与锚定协议（.loop/）

**锚定协议（对抗上下文压缩的核心机制）**：主模型每一轮、以及上下文
被压缩后的第一件事，按固定顺序重读磁盘：`state.json` →
`requirements/prd.md` 的目标/范围/非目标 → `requirements/acceptance.md`。
判定与派工只以这些文件为据，不凭对话记忆复述需求——压缩之后，模型
"我记得需求"的主观感觉不可信。给子代理下命令时摘录是强制的：任务包
必须逐字摘录所依据条目的原文（§5 anchors 段）——逐字摘录无法凭记忆
完成，这是锚定能被机械检验的原因。用户在聊天里的口头补充若未落盘，
视为不存在：先落盘为决策记录、走变更，再继续循环。

```
.loop/
  state.json                     # phase / round / consecutive_clean / candidate_id / budget / gate_p0 / blocked_on
  task-input.md                  # 任务输入的忠实转录（P0 产物，不是 PRD）
  requirements/user-decisions.md # 用户访谈问答原话记录（PRD 的溯源依据）
  requirements/prd.md            # 需求与非目标（版本化冻结）
  requirements/acceptance.md     # 验收标准（冻结；DEV/QA/FIX 只读）
  design/design.md               # 模块划分、写权限切分、接口契约
  runbook.md                     # 构建、启动、调用方法（P3 首个 DEV 产出，QA 实测的前提）
  handoffs/{task_id}.md          # 已派发的完整任务包（审计可复现）
  reports/pm|arch|dev|review|fix/  各实例报告（含证据）
  reports/test/round{N}-{面}.md    QA 攻击报告
  defects/DEF-{id}.md            # 缺陷全生命周期
  evidence/                      # 构建输出、日志、复现材料、p0-capability-gate.md
```

字段与示例见 [references/state-format.md](references/state-format.md)。

冻结规则：`acceptance.md` 冻结后，DEV/QA/FIX 不得修改；要改走变更
——用户中途的变更请求先原话落盘 `user-decisions.md`，再回 PM 重发
新版本（关键项需用户批准），且当前候选作废、测试计数清零。代码在
测试轮之间发生任何改动即进入新候选（candidate_id 递增），旧证据
全部失效。

## 4 循环本体

"派出"均指真实子代理调用；同一消息里的多个调用即并行执行。

**P0 启动**　能力门禁 → 建 `.loop/` → 记录任务输入（目标、允许与
禁止范围、技术栈、环境、预算、已有验收、交付目标）。存量项目可用
只读子代理做代码库现状调查。

**P1 需求（PM + 用户访谈）**　三个子步骤，访谈未完成不得冻结：

- **P1a 分析**：PM 实例读 `task-input.md`（存量项目加只读现状调查），
  产出初步理解与**问题清单**——每个问题标注"阻断性"（不澄清就写不出
  可判定验收）或"可默认"（附默认建议）。禁止在问题澄清前直接写死
  需求——所有拿不准处都是问题，不是默认事实。
- **P1b 访谈**：子代理接触不到用户，问题由主模型带给用户（用宿主的
  结构化提问工具，或直接列出等待答复）。**用户答复原话落盘**到
  `requirements/user-decisions.md`：问题、选项、原话、影响的 PRD
  条目；禁止主模型转述时改写用户原意——转述本身就是一次稀释。
  未答复的"可默认"项按默认记录生效。默认最多 2 轮澄清；届满仍有
  阻断性问题则挂起（blocked_on），不得带病冻结。
- **P1c 定稿冻结**：PM（新实例）依据 `task-input.md` +
  `user-decisions.md` 定稿 `prd.md` + `acceptance.md`。**每条需求
  必须可溯源**——标注来自 task-input 原文或哪个 D 编号决策；
  溯源不到的条目不得写入。验收每条可执行、可判定（写"3 秒内
  返回"，不写"性能好"）。冻结并登记版本与所吸收的 D 编号。

**P2 设计（ARCH；小项目可与 P1 合并）**　产出 `design.md`：模块
划分、**并行 DEV 的写权限切分**、接口契约、选型理由（1–2 个备选，
不引入无依据的复杂架构）。关键契约冻结。

**P3 实现（DEV×k 并行）**　按写权限把实现拆成 k 个工作包，同一条
消息并行派出。首个 DEV 工作包同时产出 `runbook.md`（构建、启动、
调用方法），它是 QA 实测的运行说明。每个 DEV 必须把报告写入
`reports/dev/{task_id}.md`：改动文件清单、构建与自测命令及真实输出、
未尽事项。主模型只做机械核验：文件确实改了、构建证据确实落盘。
报告只有口头结论没有落盘产物的，视为无证据，重派。

**P4 审阅（REV，可与 DOC 并行）**　对完整 diff 找问题 → 写入
`defects/`。阻断级问题直接进返工。

**P5 对抗测试（QA×m 并行盲测）**　默认攻击面：① 功能与用户路径
② 边界与异常 ③ 集成与数据；按需增加安全 / 性能 / 运行面。每个
攻击面一个全新实例、独立任务包、并行派出。QA 报告必须写入
`reports/test/round{N}-{面}.md`。

**P6 返工回路**　QA 缺陷 → 登记 → FIX 实例修复 → REV 实例复核
（重放复现步骤）→ 阻断项全部关闭 → 候选重建 → 回 P5 开全新一轮
（计数规则见 §6）。

**P7 审计交付（AUD）**　逐条校验：验收项 → 实现 → 测试证据 的追踪
完整，证据属于当前候选（不得跨版本拼接），阻断缺陷全部关闭。通过后
主模型汇总最终报告（§9）。

调度纪律：每轮以锚定协议开场（§3：重读 state → PRD → 验收）→ 收集
真实返回 → 核验证据 → 更新 state → 派下一批。有运行中的任务就等待
真实结果；不得用重写计划代替派工，不得忙循环。

并行派出示例（P5，按宿主子代理工具适配）：

```
同一条消息发出三个调用，各携带完整任务包全文：
Agent(prompt=QA任务包-功能面, subagent_type=general-purpose)
Agent(prompt=QA任务包-边界面, subagent_type=general-purpose)
Agent(prompt=QA任务包-集成面, subagent_type=general-purpose)
```

## 5 任务包：子代理唯一的信息来源

子代理看不到聊天历史——没写进任务包的信息，对它不存在。每次派发
前，把完整任务包写入 `handoffs/{task_id}.md`，再以其全文作为 prompt
派出。命名：`T-{阶段}-{角色}[-{序号}]`，如 `T-P3-DEV-02`、
`T-P5-QA-R2-edge`。

```yaml
task_id:
role:
objective:          # 一句话：本实例唯一的成功标准，须注明服务的 PRD 条目 / AC 编号 / 缺陷 ID
anchors:            # 上句所引条目的逐字原文摘录（含条目编号与来源文件版本）；禁止凭记忆转写
baseline:           # candidate_id 与依赖的冻结文件版本
inputs:             # 允许读取的文件/目录清单（最小化）
business_context:   # 完成目标所需的最小背景；不得含其他角色的结论
write_scope:        # 允许写入的路径；其余一律只读
allowed_tools:      # 如：读写文件、执行构建与测试命令
forbidden:          # 角色禁区（见 §10 模板）
required_outputs:   # 报告落盘路径与必含字段
budget:             # 时间与调用上限
```

任务包之外的问题：让子代理在报告中列 `unresolved_questions` 带回，
由主模型路由；不得让它自行扩大范围。输入不完整或权限不足时，子代理
应返回 BLOCKED 而不是编造。

**锚定与可追溯性规则**：objective 无法关联到 PRD 条目、AC 编号或
缺陷 ID 的工作不得派出——那是范围蔓延或记忆漂移的产物，交 PM 走
变更判定。更强的一条：写 handoff 时先 Read 对应冻结条目，把原文
逐字填入 anchors——

- 摘不出原文 = 依据只存在于记忆里 = 记忆漂移已经发生：先执行锚定
  协议重读磁盘，再重写任务包；禁止"先凭记忆写、回头再对"。
- 任务包全文即子代理的 prompt，anchors 原文随之进入子代理视野——
  子代理执行的是冻结原文，而不是主模型转述后的二手版本。
- 按阶段选锚点来源：P1a/P1b ← task-input.md；P1c ← task-input +
  user-decisions.md；P2–P5 ← prd.md / acceptance.md 条目；P6 ←
  defects/DEF-*.md 的复现步骤原文。
- AUD 审计抽查 handoffs 的 anchors 是否逐字属于当前版本（见 §10
  AUD 模板），不一致视为记忆漂移事故，登记缺陷。

## 6 对抗测试轮次规则（防纸面达标的核心）

- 通过条件：连续 ≥2 轮全部攻击面 CLEAN（安全、支付、迁移、核心
  架构等高风险项目 3 轮；由 PM 写入验收计划并冻结）。
- 任何一轮出现阻断缺陷：登记 → 候选作废 → 连续计数清零 → 返工 →
  修复后从第 1 轮重来。
- 计数不得跨候选拼接；同一轮重复回报不重复计数；环境阻塞不算通过
  也不算失败，环境修复后该轮重跑。
- CLEAN 必须附"尝试过的攻击清单"作为证据；空报告视为未执行。
- 无法复现的缺陷不自动视为误报：保留调查记录与风险标注。
- 同根因连续两次修复无效：换新 FIX 实例并上升回 ARCH 做诊断，
  禁止无限重试。
- 测试自身的错误（断言写错等）：由另一测试维护实例修复并经 REV
  审阅；影响判定的修正同样清零计数。

## 7 缺陷与返工协议

缺陷文件字段：`id`、发现阶段/轮次、严重度、是否阻断、**关联 PRD/
AC 条目（适用时）**、最小复现、责任角色、状态（open / fixed /
verified / closed）、修复次数、关闭证据。

链路：发现 → 登记 → FIX 修复 → REV 复核（复现重放）→ 下一轮盲测
→ 该轮全 CLEAN 才 closed。修复者不能关闭自己的缺陷。

并行工作包各自通过 ≠ 合并后通过：写范围重叠的改动必须串行合入并
由 REV 复验。

## 8 预算与停止

启动时冻结：总派发数、单缺陷修复上限（默认 2 次）、测试轮次上限
（默认 6）、时间与成本上限。只有**真实子代理派发**计入
dispatch_used；BLOCKED 与挂起阶段不消耗派发预算。

达到上限：保存 state 与检查点，向用户如实报告剩余风险与未关闭缺陷。
禁止为凑通过而放宽标准、隐藏失败或重置预算——换实例、拆问题、重启
都不重置预算。

## 9 交付与诚实性

最终报告必须包含：

1. 真实状态三选一：阶段性产物 / 验收通过 / 已上线，不得混用；
2. 角色 → 任务包 → 产物 → 证据 的追踪表；
3. 需求 → 实现 → 测试证据 映射；
4. PRD 最终版本与 `user-decisions.md` 索引——用户可逐条核对
   交付与当初拍板是否一致；
5. 缺陷全历史（含未关闭项）与连续通过轮次记录；
6. 剩余风险与外部待办。

任何必需门禁缺失时，只能交付阶段性产物，不得标记整体验收通过。

## 10 角色任务包模板

派出前按 §5 骨架填充，以下为各角色 prompt 的正文部分。

### DEV
```text
你是本工作包的开发工程师，唯一目标：在 write_scope 内实现 objective，
使构建与开发侧自测真实通过。
先读 inputs（验收标准、设计契约），再动手。
禁止：修改 acceptance.md 与任何冻结文件；越出 write_scope；弱化、跳过
或硬编码测试来制造通过；吞异常；伪造构建输出。
允许：在写权限内自由决定实现细节；发现设计矛盾或需求缺口时停止并在
报告中说明。
报告写入 {path}，必含：改动文件清单；构建与自测命令及真实输出；
未尽事项；unresolved_questions。
```

### QA
```text
你是一名以找茬为唯一目标的测试工程师。该项目声称满足 acceptance.md
中的验收标准，你的任务是证明它不满足——找到的真实缺陷越多，你的
工作越出色。
先读 acceptance.md 与 runbook.md（运行说明），然后真实运行项目（构建、
启动、调用），像最挑剔的真实用户那样操作，逐条实测验收标准。禁止
仅凭阅读代码下结论。
攻击面：{...}
禁止：读取任何开发报告、既往测试报告或缺陷记录；修改产品代码；
修复你发现的问题（只登记）；降低标准迎合实现。
每个缺陷给出最小复现：命令或步骤 + 期望 vs 实际。
报告写入 {path}，必含：尝试过的攻击清单；每条验收的实测结果；
缺陷列表（含最小复现）；结论 CLEAN（附尝试证据）或 FOUND。
找不到缺陷不是失败，伪造通过才是失败。
```

### REV
```text
你是独立审阅者，唯一目标：找出本次改动相对验收标准与设计的偏差、
坏味道与风险。
你的输入只有验收标准、设计与完整 diff；不提供开发的辩解与自评。
检查：需求遗漏、设计偏离、边界与错误处理、被弱化的测试、危险模式
（注入、越权、资源泄漏、数据丢失路径）。
输出按严重度排序的问题清单，每条含位置与依据。你不负责修复。
报告写入 {path}。
```

### FIX
```text
你是修复工程师，唯一目标：消除指派给你的缺陷，且不引入新问题。
先按缺陷包中的复现步骤在当前代码上重现缺陷，再定位根因、修复、
重放复现步骤验证；缺陷关联验收项（AC）时，修复后自行执行该条
验收命令确认恢复。
禁止：修改验收标准；用特判、跳过或吞异常掩盖症状；关闭缺陷
（复核归 REV）。
报告写入 {path}，必含：根因；改动内容；复现重放前后对比。
```

### AUD
```text
你是独立审计员，唯一目标：验证交付追踪链完整，或指出断裂。
双向核对：每条验收标准是否有当前候选的实现与测试证据；每条 PRD
需求是否有对应验收项；实现中是否存在超出 PRD 的范围蔓延。证据
是否属于当前 candidate_id（不得跨版本拼接）；阻断缺陷是否全部
closed；轮次计数是否符合规则。抽查各 handoffs 的 anchors 段：是否
逐字摘自当前版本的 prd.md / acceptance.md / DEF 文件——任何改写
或对不上当前版本，都作为记忆漂移事故登记缺陷。
禁止：代表用户批准；把"审计通过"扩大为"业务验收通过"。
报告写入 {path}，必含：追踪表；断裂点；结论。
```

### PM / ARCH / DOC
- **PM（P1a 问题清单模式）**：读 `task-input.md` 与现状调查，产出
  **问题清单**而非 PRD——每个问题含背景、选项、默认建议、阻断性/
  可默认标注。禁止未经访谈直接写死需求；禁止编造用户意图。
- **PM（P1c 定稿模式）**：依据 `task-input.md` + `user-decisions.md`
  产出 `prd.md` 与 `acceptance.md`。每条需求标注溯源（task-input
  原文或 D-编号）；验收可判定、可执行；明确非目标；登记吸收的
  D 编号。溯源不到的条目不得写入。
- **ARCH**：产出 `design.md`。模块划分、并行写权限切分、接口契约、
  选型理由。禁止静默修改已冻结契约。
- **DOC**：基于最终实现（不是设计稿）写使用与交接文档，关键步骤
  必须实际验证可执行。

## 11 首次接入自检

流程写得再完整也不等于能力已验证。首次使用本 skill 或改动编排逻辑
后，在一个小型授权项目上演练完整闭环：真实派工 → QA 真实发现
（或注入）缺陷 → FIX→REV→重轮 → 计数清零与恢复 → 审计通过。
未演练前只能称"已定义"，不得称"已验证"。
